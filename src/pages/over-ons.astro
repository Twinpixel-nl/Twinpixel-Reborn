---
import BaseLayout from "../layouts/BaseLayout.astro";
import Button from "../components/Button.astro";

const title = "Over ons | TwinPixel - technisch webbureau uit Wageningen";
const description =
  "TwinPixel is een technisch webbureau uit Wageningen. We bouwen snelle, onderhoudbare websites en webapps met focus op performance, structuur en SEO.";

const heroImageSrc = "/zelfverzekerd.webp";
const heroImageAlt = "Twan en Emma van TwinPixel in Wageningen";

const siteUrl = "https://twinpixel.nl";
---

<BaseLayout title={title} description={description}>
  <!-- Verwijzing naar hoofd-entity uit BaseLayout -->
  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "AboutPage",
      mainEntity: {
        "@id": `${siteUrl}#organization`,
      },
    })}
  ></script>

  <main class="page">
    <!-- HERO -->
    <header class="hero-section">
      <div class="hero-content">
        <div class="hero-text">
          <p class="kicker fade-item" style="animation-delay: 0.1s">Over TwinPixel</p>

          <h1 class="hero-title fade-item" style="animation-delay: 0.2s">Technisch webbureau uit Wageningen</h1>

          <p class="hero-description fade-item" style="animation-delay: 0.3s">
            Wij bouwen websites en webapps die snel laden, logisch zijn opgebouwd en makkelijk te onderhouden blijven.
            Je werkt direct met ons, geen accountmanager ertussen. Vaak voor mkb en organisaties die een nieuwe site nodig hebben,
            of een bestaande website technisch strak willen trekken.
          </p>

          <div class="hero-bullets" aria-label="Kernpunten">
            <div class="bullet"><span class="dot" aria-hidden="true"></span> Core Web Vitals & performance als basis</div>
            <div class="bullet"><span class="dot" aria-hidden="true"></span> Structuur, UX en duidelijke flows</div>
            <div class="bullet"><span class="dot" aria-hidden="true"></span> Technische SEO: titles, schema en indexatie</div>
          </div>
        </div>

        <figure class="hero-image">
          <img
            src={heroImageSrc}
            alt={heroImageAlt}
            width="1200"
            height="900"
            loading="eager"
            fetchpriority="high"
            decoding="async"
          />
          <figcaption class="image-caption">
            Wij combineren strategie, design en techniek tot websites die werken.
          </figcaption>
        </figure>
      </div>
    </header>

 <!-- WIE WIJ ZIJN -->
<section class="about-section" aria-label="Wie wij zijn">
  <div class="about-grid">

<div class="about-text">
<p>
  TwinPixel is een technisch webbureau uit Wageningen. We bouwen websites voor mkb en organisaties
  die niet alleen mooi zijn, maar ook snel laden, logisch zijn opgebouwd en duurzaam ontwikkeld worden.
</p>

<p>
  We ontwerpen én bouwen zelf. Daardoor blijven keuzes helder, blijft de scope scherp
  en leveren we websites op die technisch kloppen en jarenlang mee kunnen.
</p>

<p class="subtle">
  We werken vanuit Wageningen (Foodvalley), maar net zo goed remote.
  Het resultaat is belangrijker dan de locatie.
</p>


  <p class="links">
    <a href="/webdesign-wageningen">Webdesign Wageningen</a>
    <span aria-hidden="true"> · </span>
    <a href="/expertise">Expertise</a>
    <span aria-hidden="true"> · </span>
    <a href="/cases">Cases</a>
  </p>
</div>

<!-- RECHTER KOLOM -->
<div class="team-mini" aria-label="Team">


  <article class="team-person">
    <img class="team-avatar" src="/EmmaFoto.webp" alt="Emma van TwinPixel" width="100" height="100" loading="lazy" />
    <div class="team-meta">
      <h2 class="team-name">Emma</h2>
      <p class="team-role">UX Designer</p>
      <p class="team-note">
        Ontwerpt de structuur en gebruikersflows.
        Zorgt dat bezoekers begrijpen wat ze moeten doen.
      </p>
    </div>
  </article>

  
  <article class="team-person">
    <img class="team-avatar" src="/TwanFoto.webp" alt="Twan van TwinPixel" width="100" height="100" loading="lazy" />
    <div class="team-meta">
      <h2 class="team-name">Twan</h2>
      <p class="team-role">Technical Lead</p>
      <p class="team-note">
        Verantwoordelijk voor architectuur, performance en technische keuzes.
        Zorgt dat alles snel, schaalbaar en onderhoudbaar blijft.
      </p>
    </div>
  </article>

  </div>
</div>
</section>

<!-- WERKWIJZE -->
<section class="values-section" aria-label="Werkwijze TwinPixel">

  <div class="values-grid">
    <div class="value-card">
      <h2 class="value-title">Waar we op letten</h2>
      <ul class="value-list">
        <li>
          <strong>Structuur en duidelijkheid:</strong>
          één heldere hoofdactie per pagina en logische informatie-opbouw.
        </li>
        <li>
          <strong>Performance:</strong>
          snelle laadtijden, geoptimaliseerde assets en sterke Core Web Vitals.
        </li>
        <li>
          <strong>Technische kwaliteit:</strong>
          schone code, duidelijke architectuur en onderhoudbare opzet.
        </li>
        <li>
          <strong>Schaalbaarheid:</strong>
          websites en webapplicaties die mee kunnen groeien zonder breekwerk.
        </li>
      </ul>
    </div>

    <div class="value-card">
      <h2 class="value-title">Onze werkwijze</h2>
      <ol class="process-list">
        <li>
          <span class="step-number">01</span>
          Analyse & technische inventarisatie
        </li>
        <li>
          <span class="step-number">02</span>
          Structuur, UX en functioneel ontwerp
        </li>
        <li>
          <span class="step-number">03</span>
          Visual design en componentuitwerking
        </li>
        <li>
          <span class="step-number">04</span>
          Development, testing & performance optimalisatie
        </li>
      </ol>
    </div>

  </div>
</section>

    <!-- CTA -->
    <section class="cta-section" aria-label="Call to action">
      <div class="cta-content">
        <p class="cta-kicker">Samenwerken?</p>
        <h2 class="cta-title">Stuur je idee en we maken een concreet plan</h2>
        <p class="cta-description">
          Deel je doel, planning en context. Dan reageren we met een voorstel en een heldere aanpak.
        </p>

        <div class="cta-actions">
          <Button href="/contact" variant="primary" label="Plan kennismaking" />
          <a class="cta-link" href="/cases">Bekijk cases</a>
        </div>
      </div>
    </section>
  </main>

  <style>
.page {
  max-width: 1200px;
  margin: 0 auto;
  padding: 4rem 1.5rem 6rem;
  display: grid;
  gap: 4rem;
}

/* Hero */
.hero-section {
  padding: 2rem 0;
}

.hero-content {
  display: grid;
  grid-template-columns: 1.05fr 0.95fr;
  gap: 4rem;
  align-items: start; /* belangrijk: matcht beter met about */
}

.hero-text {
  display: grid;
  gap: 1.25rem;
}

.kicker {
  text-transform: uppercase;
  font-size: 0.875rem;
  letter-spacing: 0.1em;
  color: rgba(255, 255, 255, 0.7);
  font-weight: 500;
  margin: 0;
}

.hero-title {
  font-size: clamp(2.4rem, 4vw, 3.4rem);
  line-height: 1.08;
  font-weight: 700;
  color: #fff;
  margin: 0;
  letter-spacing: -0.02em;
}

.hero-description {
  font-size: 1.125rem;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.9);
  margin: 0;
  max-width: 62ch;
}

.hero-bullets {
  display: grid;
  gap: 0.6rem;
  margin-top: 0.25rem;
  padding-top: 0.75rem;
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  max-width: 62ch;
}

.bullet {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.5;
  font-size: 0.98rem;
}

.dot {
  width: 8px;
  height: 8px;
  border-radius: 999px;
  background: #2f89ff;
  box-shadow: 0 0 0 3px rgba(47, 137, 255, 0.18);
  flex: 0 0 auto;
}

.hero-image {
  margin: 0;
  border-radius: 20px;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.08);
  background: rgba(255, 255, 255, 0.02);
}

.hero-image img {
  width: 100%;
  height: auto;
  display: block;
  object-fit: cover;
  aspect-ratio: 4/3;
}

.image-caption {
  margin: 0;
  padding: 0.95rem 1.2rem;
  font-size: 0.95rem;
  color: rgba(255, 255, 255, 0.75);
  line-height: 1.5;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  background: rgba(0, 0, 0, 0.15);
}

/* Shared */
.subtle {
  color: rgba(255, 255, 255, 0.75);
}

.links a {
  color: rgba(255, 255, 255, 0.92);
  text-decoration: none;
  border-bottom: 1px solid rgba(255, 255, 255, 0.25);
}

.links a:hover {
  color: #fff;
  border-bottom-color: rgba(255, 255, 255, 0.55);
}

/* About: exact dezelfde middenlijn als hero */
.about-grid {
  display: grid;
  grid-template-columns: 1.05fr 0.95fr; /* exact match met hero */
  gap: 4rem;
  align-items: start;
}

/* Jij gebruikt .about-text in je HTML */
.about-text {
  max-width: 65ch;
  display: grid;
  gap: 1.25rem;
}

.about-text p {
  margin: 0;
  font-size: 1.08rem;
  line-height: 1.75;
  color: rgba(255, 255, 255, 0.86);
}

/* Team mini: rechts in lijn met andere cards */
.team-mini {
  justify-self: end;
  width: 100%;
  max-width: 720px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  padding: 2rem;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.02);
}

.team-person {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 1rem;
}

.team-avatar {
  width: 100px;
  height: 100px;
  border-radius: 999px;
  object-fit: cover;
  border: 1px solid rgba(255, 255, 255, 0.12);
  background: rgba(255, 255, 255, 0.03);
}

.team-meta {
  display: grid;
  gap: 0.35rem;
}

.team-name {
  margin: 0;
  color: #fff;
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: -0.01em;
}

.team-role {
  margin: 0;
  color: rgba(255, 255, 255, 0.78);
  font-weight: 600;
  line-height: 1.5;
  font-size: 1rem;
}

.team-note {
  margin: 0.25rem 0 0;
  color: rgba(255, 255, 255, 0.75);
  line-height: 1.6;
  font-size: 0.98rem;
}

/* Values */
.values-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
}

.value-card {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 16px;
  padding: 2rem;
  transition: transform 0.25s ease, border-color 0.25s ease, background 0.25s ease;
}

.value-card:hover {
  background: rgba(255, 255, 255, 0.05);
  border-color: rgba(255, 255, 255, 0.12);
  transform: translateY(-2px);
}

.value-title {
  font-size: 1.25rem;
  font-weight: 650;
  color: #fff;
  margin: 0 0 1.25rem 0;
  letter-spacing: -0.01em;
}

.value-list {
  margin: 0;
  padding: 0;
  list-style: none;
  display: grid;
  gap: 0.95rem;
}

.value-list li {
  color: rgba(255, 255, 255, 0.82);
  line-height: 1.65;
  padding-left: 1.5rem;
  position: relative;
}

.value-list li::before {
  content: "→";
  position: absolute;
  left: 0;
  color: #2f89ff;
  font-weight: 800;
}

.process-list {
  margin: 0;
  padding: 0;
  list-style: none;
  display: grid;
  gap: 2.5rem;
}

.process-list li {
  color: rgba(255, 255, 255, 0.82);
  line-height: 1.65;
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

.step-number {
  background: linear-gradient(135deg, #2f89ff, rgba(47, 137, 255, 0.8));
  color: #fff;
  font-size: 0.75rem;
  font-weight: 700;
  padding: 0.25rem 0.6rem;
  border-radius: 999px;
  min-width: 36px;
  text-align: center;
  flex-shrink: 0;
  margin-top: 0.1rem;
}

/* CTA */
.cta-section {
  margin-top: 0.5rem;
}

.cta-content {
  background: linear-gradient(135deg, rgba(47, 137, 255, 0.1), rgba(47, 137, 255, 0.05));
  border: 1px solid rgba(47, 137, 255, 0.2);
  border-radius: 20px;
  padding: 3rem;
  text-align: center;
  max-width: 680px;
  margin: 0 auto;
}

.cta-kicker {
  text-transform: uppercase;
  font-size: 0.875rem;
  letter-spacing: 0.1em;
  color: rgba(255, 255, 255, 0.7);
  font-weight: 500;
  margin: 0 0 1rem 0;
}

.cta-title {
  font-size: clamp(1.6rem, 2.2vw, 2rem);
  font-weight: 700;
  color: #fff;
  margin: 0 0 1.25rem 0;
  line-height: 1.2;
  letter-spacing: -0.01em;
}

.cta-description {
  font-size: 1.08rem;
  color: rgba(255, 255, 255, 0.86);
  margin: 0 0 2rem 0;
  line-height: 1.65;
  max-width: 56ch;
  margin-left: auto;
  margin-right: auto;
}

.cta-actions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.cta-link {
  display: inline-flex;
  align-items: center;
  padding: 0.875rem 1.5rem;
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 999px;
  text-decoration: none;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.92);
  transition: background 0.25s ease, border-color 0.25s ease, color 0.25s ease;
}

.cta-link:hover {
  border-color: rgba(255, 255, 255, 0.45);
  background: rgba(255, 255, 255, 0.05);
  color: #fff;
}

/* Responsive */
@media (max-width: 1024px) {
  .page {
    gap: 3rem;
    padding: 3rem 1.5rem 4.5rem;
  }

  .hero-content {
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .about-grid {
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .team-mini {
    justify-self: start;
    max-width: 720px;
  }

  .about-text {
    max-width: 72ch;
  }
}

@media (max-width: 768px) {
  .page {
    gap: 2.5rem;
    padding: 2.5rem 1rem 3.25rem;
  }

  .hero-content {
    text-align: center;
  }

  .hero-description,
  .hero-bullets {
    margin-left: auto;
    margin-right: auto;
  }

  .values-grid {
    grid-template-columns: 1fr;
  }

  .team-mini {
    grid-template-columns: 1fr;
  }

  .value-card {
    padding: 1.5rem;
  }

  .cta-content {
    padding: 2rem 1.5rem;
  }

  .cta-actions {
    flex-direction: column;
    align-items: center;
  }
}

@media (max-width: 480px) {
  .page {
    padding: 2rem 1rem 2.75rem;
    gap: 2rem;
  }

  .hero-title {
    font-size: 2rem;
  }

  .cta-content {
    padding: 1.5rem 1rem;
  }
}


  </style>
</BaseLayout>
