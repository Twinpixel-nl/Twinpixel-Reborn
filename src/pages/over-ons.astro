---
import BaseLayout from "../layouts/BaseLayout.astro";
import Button from "../components/Button.astro";

const title = "Over Ons";
const description = "Wij zijn twee HBO-ICT studenten met focus op technische websites.";
const teamImage = "/images/team-sparring.jpg"; // plaats je foto hier
---

<BaseLayout title={title} description={description}>
  <main class="page">
    <!-- Hero Section -->
    <section class="hero-section fade-in-hero">
      <div class="hero-content">
        <div class="hero-text">
          <p class="kicker fade-item" style="animation-delay: 0.1s">Wie Wij Zijn</p>
          <h1 class="hero-title fade-item" style="animation-delay: 0.2s">Samen bouwen aan duidelijke, snelle websites</h1>
          <p class="hero-description fade-item" style="animation-delay: 0.3s">
            Twee HBO-ICT studenten met een focus op techniek. We combineren design, performance
            en onderhoudbaarheid zodat je site er strak uitziet én snel blijft.
          </p>
        </div>

        <div class="hero-image">
          <img src="/zelfverzekerd.webp" alt="Twee teamleden van TwinPixel" loading="lazy" />
          <p class="image-caption">Wij combineren strategie, design en techniek tot websites die werken.</p>
        </div>
      </div>
    </section>

    <!-- About Content -->
    <section class="about-section">
      <div class="about-content">
        <p>
          Wij zijn twee HBO-ICT studenten en bouwen naast onze studie websites
          voor ondernemers en organisaties. We focussen ons op techniek:
          snelheid, structuur en onderhoudbaarheid.
        </p>

        <p>
          Geen templates en geen marketingbureau-praat. Je werkt direct
          met de mensen die je website ontwerpen en bouwen. Dat houdt het
          overzichtelijk en efficiënt.
        </p>

        <p>
          We zijn gevestigd in Wageningen en werken vooral met klanten
          die iets degelijks willen neerzetten, nu en op de lange termijn.
        </p>
      </div>
    </section>

    <!-- Values & Process -->
    <section class="values-section">
      <div class="values-grid">
        <div class="value-card">
          <h2 class="value-title">Waar We In Geloven</h2>
          <ul class="value-list">
            <li><strong>Helder design:</strong> elke pagina krijgt een duidelijke hiërarchie en CTA.</li>
            <li><strong>Performance-first:</strong> Astro, geoptimaliseerde assets en focus op Core Web Vitals.</li>
            <li><strong>Onderhoudbaar:</strong> component-gedreven UI zodat uitbreiden geen breekwerk wordt.</li>
            <li><strong>Transparant samenwerken:</strong> korte lijntjes, vaste contactpersonen, duidelijke planning.</li>
          </ul>
        </div>

        <div class="value-card">
          <h2 class="value-title">Hoe We Werken</h2>
          <ol class="process-list">
            <li><span class="step-number">01</span> <strong>Inventarisatie:</strong> doelen, doelgroep, content en bestaande techniek.</li>
            <li><span class="step-number">02</span> <strong>Structuur & UX:</strong> flow, wireframes en contentindeling zodat keuzes helder zijn.</li>
            <li><span class="step-number">03</span> <strong>Design:</strong> look & feel, componenten en states uitgewerkt voor desktop en mobiel.</li>
            <li><span class="step-number">04</span> <strong>Build & checks:</strong> snelle build, SEO-basics, performance- en accessibility checks.</li>
          </ol>
        </div>
      </div>
    </section>

    <!-- Info Cards -->
    <section class="info-section">
      <div class="info-grid">
        <div class="info-card">
          <h3>Klanttypes</h3>
          <p>
            B2B-services, agencies, SaaS/portals en mkb die een professionele, snelle site nodig hebben
            of complexe flows willen vereenvoudigen.
          </p>
        </div>

        <div class="info-card">
          <h3>Wat Klanten Waarderen</h3>
          <p>
            Heldere communicatie, snelle iteraties, meedenken over content/SEO en een oplevering die echt
            snel laadt.
          </p>
        </div>

        <div class="info-card">
          <h3>Tooling</h3>
          <p>
            Astro, Tailwind/vanilla CSS, Figma, Netlify/Vercel, integraties met CRM/analytics en automatisering
            waar het zinvol is.
          </p>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="cta-content">
        <p class="cta-kicker">Samenwerken?</p>
        <h2 class="cta-title">Stuur je idee en we maken een concreet plan</h2>
        <p class="cta-description">
          Zet je doel, scope en gewenste opleverdatum erbij. We reageren snel met voorstel, planning en
          wat we meenemen qua design, performance en SEO.
        </p>
        <div class="cta-actions">
          <Button href="/contact" variant="primary" label="Plan Kennismaking" />
          <a class="cta-link" href="/cases">Bekijk Cases</a>
        </div>
      </div>
    </section>
  </main>

  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .fade-item {
      opacity: 0;
      animation: fadeInUp 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .page {
      max-width: 1200px;
      margin: 0 auto;
      padding: 4rem 1.5rem 6rem;
      display: grid;
      gap: 4rem;
    }

    /* Hero Section */
    .hero-section {
      padding: 2rem 0;
    }

    .hero-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    .hero-text {
      display: grid;
      gap: 1.5rem;
    }

    .kicker {
      text-transform: uppercase;
      font-size: 0.875rem;
      letter-spacing: 0.1em;
      color: rgba(255,255,255,0.7);
      font-weight: 500;
      margin: 0;
    }

    .hero-title {
      font-size: clamp(2.5rem, 4vw, 3.5rem);
      line-height: 1.1;
      font-weight: 700;
      color: #fff;
      margin: 0;
      letter-spacing: -0.02em;
    }

    .hero-description {
      font-size: 1.125rem;
      line-height: 1.7;
      color: rgba(255,255,255,0.9);
      margin: 0;
      max-width: 60ch;
    }

    .hero-image {
      border-radius: 20px;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,0.08);
      background: rgba(255,255,255,0.02);
    }

    .hero-image img {
      width: 100%;
      height: auto;
      display: block;
      object-fit: cover;
      aspect-ratio: 4/3;
    }

    .image-caption {
      margin: 0;
      padding: 1rem 1.25rem;
      font-size: 0.95rem;
      color: rgba(255,255,255,0.75);
      line-height: 1.5;
      border-top: 1px solid rgba(255,255,255,0.06);
    }

    /* About Section */
    .about-section {
      padding: 2rem 0;
    }

    .about-content {
      max-width: 70ch;
      display: grid;
      gap: 1.5rem;
    }

    .about-content p {
      margin: 0;
      font-size: 1.1rem;
      line-height: 1.7;
      color: rgba(255,255,255,0.85);
    }

    /* Values Section */
    .values-section {
      padding: 2rem 0;
    }

    .values-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }

    .value-card {
      background: rgba(255,255,255,0.03);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 16px;
      padding: 2rem;
      transition: all 0.3s ease;
    }

    .value-card:hover {
      background: rgba(255,255,255,0.05);
      border-color: rgba(255,255,255,0.12);
      transform: translateY(-2px);
    }

    .value-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: #fff;
      margin: 0 0 1.5rem 0;
      letter-spacing: -0.01em;
    }

    .value-list {
      margin: 0;
      padding: 0;
      list-style: none;
      display: grid;
      gap: 1rem;
    }

    .value-list li {
      color: rgba(255,255,255,0.8);
      line-height: 1.6;
      padding-left: 1.5rem;
      position: relative;
    }

    .value-list li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: #2F89FF;
      font-weight: bold;
    }

    .process-list {
      margin: 0;
      padding: 0;
      list-style: none;
      display: grid;
      gap: 1.25rem;
    }

    .process-list li {
      color: rgba(255,255,255,0.8);
      line-height: 1.6;
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .step-number {
      background: linear-gradient(135deg, #2F89FF, rgba(47,137,255,0.8));
      color: white;
      font-size: 0.75rem;
      font-weight: 600;
      padding: 0.25rem 0.5rem;
      border-radius: 12px;
      min-width: 24px;
      text-align: center;
      flex-shrink: 0;
      margin-top: 0.125rem;
    }

    /* Info Section */
    .info-section {
      padding: 2rem 0;
    }

    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
    }

    .info-card {
      background: rgba(255,255,255,0.02);
      border: 1px solid rgba(255,255,255,0.06);
      border-radius: 16px;
      padding: 2rem;
      transition: all 0.3s ease;
    }

    .info-card:hover {
      background: rgba(255,255,255,0.04);
      border-color: rgba(255,255,255,0.1);
    }

    .info-card h3 {
      font-size: 1.1rem;
      font-weight: 600;
      color: #fff;
      margin: 0 0 1rem 0;
      letter-spacing: -0.01em;
    }

    .info-card p {
      margin: 0;
      color: rgba(255,255,255,0.8);
      line-height: 1.6;
    }

    /* CTA Section */
    .cta-section {
      padding: 3rem 0;
      margin-top: 2rem;
    }

    .cta-content {
      background: linear-gradient(135deg, rgba(47,137,255,0.1), rgba(47,137,255,0.05));
      border: 1px solid rgba(47,137,255,0.2);
      border-radius: 20px;
      padding: 3rem;
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }

    .cta-kicker {
      text-transform: uppercase;
      font-size: 0.875rem;
      letter-spacing: 0.1em;
      color: rgba(255,255,255,0.7);
      font-weight: 500;
      margin: 0 0 1rem 0;
    }

    .cta-title {
      font-size: 2rem;
      font-weight: 600;
      color: #fff;
      margin: 0 0 1.5rem 0;
      line-height: 1.2;
      letter-spacing: -0.01em;
    }

    .cta-description {
      font-size: 1.1rem;
      color: rgba(255,255,255,0.85);
      margin: 0 0 2rem 0;
      line-height: 1.6;
      max-width: 50ch;
      margin-left: auto;
      margin-right: auto;
    }

    .cta-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .cta-link {
      display: inline-flex;
      align-items: center;
      padding: 0.875rem 1.5rem;
      border: 1px solid rgba(255,255,255,0.2);
      border-radius: 50px;
      text-decoration: none;
      font-weight: 500;
      color: rgba(255,255,255,0.9);
      transition: all 0.3s ease;
    }

    .cta-link:hover {
      border-color: rgba(255,255,255,0.4);
      background: rgba(255,255,255,0.05);
      color: #fff;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
      .page {
        gap: 3rem;
        padding: 3rem 1.5rem 4rem;
      }

      .hero-content {
        gap: 3rem;
      }

      .values-grid {
        gap: 1.5rem;
      }
    }

    @media (max-width: 768px) {
      .page {
        gap: 2.5rem;
        padding: 2.5rem 1rem 3rem;
      }

      .hero-content {
        grid-template-columns: 1fr;
        gap: 2.5rem;
        text-align: center;
      }

      .hero-title {
        font-size: 2.5rem;
      }

      .hero-description {
        font-size: 1rem;
      }

      .values-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }

      .value-card {
        padding: 1.5rem;
      }

      .info-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }

      .info-card {
        padding: 1.5rem;
      }

      .cta-content {
        padding: 2rem 1.5rem;
      }

      .cta-title {
        font-size: 1.5rem;
      }

      .cta-description {
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      .page {
        padding: 2rem 1rem 2.5rem;
        gap: 2rem;
      }

      .hero-section,
      .about-section,
      .values-section,
      .info-section {
        padding: 1rem 0;
      }

      .hero-title {
        font-size: 2rem;
      }

      .value-card,
      .info-card {
        padding: 1.25rem;
      }

      .cta-content {
        padding: 1.5rem 1rem;
      }

      .cta-actions {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</BaseLayout>
