---
import BaseLayout from "../layouts/BaseLayout.astro";

const page = {
  title: "Contact",
  description: "Stuur ons een bericht. We reageren meestal binnen 1 werkdag.",
};

const company = {
  name: "TwinPixel",
  location: "Wageningen, Nederland",
  email: "info@twinpixel.nl",
  phone: "+31 6 82579179",
  instagram: "https://www.instagram.com/twinpixel.nl/",
  linkedin: "https://www.linkedin.com/company/twinpixel",
};
---

<BaseLayout title={page.title} description={page.description}>
  <main class="page">
    <section class="hero fade-in-hero">
      <div class="container">
        <h1 class="h1 fade-item" style="animation-delay: 0.1s">Contact</h1>
        <p class="lead fade-item" style="animation-delay: 0.2s">Stuur ons een bericht. We reageren meestal binnen 1 werkdag.</p>
      </div>
    </section>

    <section class="grid" aria-label="Contact">
      <!-- left: info -->
      <aside class="panel info" aria-label="Contactgegevens">
        <div class="card">
          <h2 class="h2">Gegevens</h2>

          <dl class="list">
            <div class="row">
              <dt>🏢 Bedrijf</dt>
              <dd>{company.name}</dd>
            </div>
            <div class="row">
              <dt>📍 Locatie</dt>
              <dd>{company.location}</dd>
            </div>
            <div class="row">
              <dt>📧 Mail</dt>
              <dd>
                <a class="link" href={`mailto:${company.email}`}>{company.email}</a>
              </dd>
            </div>
            <div class="row">
              <dt>📞 Telefoon</dt>
              <dd>
                <a class="link" href={`tel:${company.phone.replaceAll(" ", "")}`}>
                  {company.phone}
                </a>
              </dd>
            </div>
          </dl>

          <div class="actions">
            <a class="btn" href={`mailto:${company.email}?subject=Contact%20via%20website`}>
              ✉️ Mailen
            </a>
          </div>
        </div>

        <div class="card">
          <h2 class="h2">Social</h2>
          <div class="social">
            <a class="pill" href={company.linkedin} target="_blank" rel="noreferrer">
              🔗 LinkedIn
            </a>
            <a class="pill" href={company.instagram} target="_blank" rel="noreferrer">
              📸 Instagram
            </a>
          </div>

          <p class="note">
            Liever direct plannen? Zet in je bericht alvast: budget, deadline en wat je nodig hebt
            (Website, redesign, landing page, etc.).
          </p>
        </div>
      </aside>

      <!-- right: form -->
      <section class="panel form" aria-label="Contactformulier">
        <div class="card">
          <h2 class="h2">Stuur Een Bericht</h2>
          <p class="muted">
            Geen gedoe. Gewoon even invullen, dan reageren we snel.
          </p>
{Astro.url.searchParams.get("success") === "1" && (
  <p class="success" role="status">TDank voor je bericht. We reageren meestal binnen 1 werkdag.</p>
)}

           <form
    class="contact-form"
    name="contact"
   method="POST"
   action="/contact?success=1"
   data-netlify="true"
   data-netlify-honeypot="bot-field"
>

            <input type="hidden" name="form-name" value="contact" />
            <p class="sr-only">
              <label>
                Don’t fill this out if you’re human: <input name="bot-field" />
              </label>
            </p>

            <div class="fields">
              <label class="field">
                <span>Naam</span>
                <input
                  name="name"
                  type="text"
                  autocomplete="name"
                  required
                  placeholder="Je Naam"
                />
              </label>

              <label class="field">
                <span>E-mail</span>
                <input
                  name="email"
                  type="email"
                  autocomplete="email"
                  required
                  placeholder="Naam@bedrijf.nl"
                />
              </label>

              <label class="field">
                <span>Onderwerp</span>
                <input
                  name="subject"
                  type="text"
                  required
                  placeholder="Bijv. Nieuwe Website Voor Mijn Bedrijf"
                />
              </label>

              <label class="field full">
                <span>Bericht</span>
                <textarea
                  name="message"
                  rows="7"
                  required
                  placeholder="Vertel kort wat je zoekt + eventuele deadline"
                ></textarea>
              </label>
            </div>

            <div class="form-footer">
              <button class="btn" type="submit">Versturen</button>
              <p class="tiny">
                Door te versturen ga je akkoord dat we je bericht mogen gebruiken om contact op te
                nemen.
              </p>
            </div>
          </form>
        </div>

       
      </section>
    </section>
  </main>

  <style>
    .page {
      width: min(var(--container), calc(100% - 2rem));
      margin: 0 auto;
      padding: 0 0 5rem;
    }

    .hero {
      padding: 3.5rem 0 2rem;
      text-align: center;
    }

    .h1 {
      font-size: clamp(2rem, 3vw, 2.8rem);
      line-height: 1.1;
      margin: 0 0 0.75rem;
      color: var(--text);
      opacity: 0;
      animation: fadeInUp 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards;
    }

    .lead {
      margin: 0;
      max-width: 60ch;
      opacity: 0;
      animation: fadeInUp 0.7s cubic-bezier(0.22, 1, 0.36, 1) forwards;
      color: var(--muted);
      margin: 0 auto;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr 1.2fr;
      gap: 1.25rem;
      align-items: start;
    }

    .panel {
      display: grid;
      gap: 1rem;
    }

    .card {
      border: 1px solid var(--stroke);
      background: var(--card);
      border-radius: var(--radius);
      padding: 1.5rem;
      backdrop-filter: blur(8px);
      transition: all 0.3s ease;
    }

    .card:hover {
      border-color: rgba(255, 255, 255, 0.2);
      background: rgba(255, 255, 255, 0.06);
    }

    .subtle {
      background: rgba(255, 255, 255, 0.02);
    }

    .h2 {
      margin: 0 0 1rem;
      font-size: 1.2rem;
      letter-spacing: 0.02em;
      color: var(--text);
    }

    .h3 {
      margin: 0 0 0.75rem;
      font-size: 1.1rem;
      color: var(--text);
    }

    .muted {
      margin: 0 0 1rem;
      color: var(--muted);
    }

    .list {
      margin: 0;
      display: grid;
      gap: 0.8rem;
    }

    .row {
      display: grid;
      grid-template-columns: 120px 1fr;
      gap: 0.75rem;
      align-items: baseline;
    }

    dt {
      color: var(--muted);
      font-size: 0.9rem;
      font-weight: 500;
    }

    dd {
      margin: 0;
      color: var(--text);
    }

    .link {
      text-decoration: none;
      border-bottom: 1px solid rgba(255, 255, 255, 0.25);
      transition: border-color 0.2s ease;
    }

    .link:hover {
      border-bottom-color: var(--brand);
    }

    .actions {
      display: flex;
      gap: 0.6rem;
      margin-top: 1.5rem;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 0.8rem 1rem;
      border-radius: 999px;
      border: 1px solid var(--stroke);
      background: var(--card);
      text-decoration: none;
      cursor: pointer;
      font-weight: 600;
      color: var(--text);
      transition: all 0.2s ease;
    }

    .btn:hover {
      background: var(--brand);
      border-color: var(--brand);
      color: white;
    }

    .btn.ghost {
      background: transparent;
    }

    .btn.ghost:hover {
      background: var(--card);
    }

    .social {
      display: flex;
      gap: 0.6rem;
      flex-wrap: wrap;
      margin-bottom: 1rem;
    }

    .pill {
      padding: 0.6rem 0.9rem;
      border-radius: 999px;
      border: 1px solid var(--stroke);
      text-decoration: none;
      background: var(--card);
      color: var(--text);
      transition: all 0.2s ease;
    }

    .pill:hover {
      background: var(--brand);
      border-color: var(--brand);
      color: white;
    }

    .note {
      margin: 0;
      color: var(--muted2);
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .contact-form {
      margin-top: 0.25rem;
    }

    .fields {
      display: grid;
      gap: 1rem;
      grid-template-columns: 1fr 1fr;
    }

    .field {
      display: grid;
      gap: 0.5rem;
    }

    .field.full {
      grid-column: 1 / -1;
    }

    .field span {
      font-size: 0.9rem;
      color: var(--muted);
      font-weight: 500;
    }

    input,
    textarea {
      width: 100%;
      padding: 0.8rem 0.9rem;
      border-radius: var(--radius);
      border: 1px solid var(--stroke);
      background: rgba(0, 0, 0, 0.2);
      color: var(--text);
      outline: none;
      transition: all 0.2s ease;
      font-family: inherit;
      font-size: 1rem;
    }

    input:focus,
    textarea:focus {
      border-color: var(--brand);
      background: rgba(0, 0, 0, 0.3);
      box-shadow: 0 0 0 3px rgba(47, 137, 255, 0.1);
    }

    .form-footer {
      display: grid;
      gap: 0.7rem;
      margin-top: 1.5rem;
    }

    .tiny {
      margin: 0;
      color: var(--muted2);
      font-size: 0.85rem;
      line-height: 1.4;
    }

    .faq {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      gap: 1rem;
    }

    .faq li {
      display: grid;
      gap: 0.25rem;
    }

    .q {
      font-weight: 650;
      color: var(--text);
    }

    .a {
      color: var(--muted);
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border-width: 0;
    }

    @media (max-width: 900px) {
      .grid {
        grid-template-columns: 1fr;
      }
      .fields {
        grid-template-columns: 1fr;
      }
      .hero {
        padding: 2rem 0 1rem;
      }
      .card {
        padding: 1.25rem;
      }
    }

    @media (max-width: 450px) {
      .row {
        grid-template-columns: 1fr;
        gap: 0.25rem;
      }
    }
    .btn:focus-visible,
.pill:focus-visible,
.link:focus-visible {
  outline: 2px solid var(--brand);
  outline-offset: 3px;
  border-radius: 999px;
}

@media (prefers-reduced-motion: reduce) {
  .h1, .lead { animation: none !important; opacity: 1 !important; }
}
@media (hover: hover) and (pointer: fine) {
  .card:hover {
    border-color: rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.06);
  }
}

  </style>
</BaseLayout>
