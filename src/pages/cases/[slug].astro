---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Button from "../../components/Button.astro";
import { cases } from "../../data/cases.js";

export function getStaticPaths() {
  return cases.map((c) => ({
    params: { slug: c.slug },
    props: { c },
  }));
}

const { c } = Astro.props;
---

<BaseLayout title={`Twinpixel | ${c.title}`} description={c.description}>
  <!-- De accentkleur wordt hier dynamisch toegepast op de hero -->
  <section class="hero" style={`--accent:${c.accent || "#2F89FF"};`}>
    <div class="container inner">
      <p class="kicker">Case Study</p>
      <h1 class="h1">{c.title}</h1>
      <p class="lead">{c.description}</p>

      <div class="cta">
        <Button href="/cases" variant="secondary" label="← Terug naar cases" />
        <Button href="/#contact" variant="primary" label="Project starten" />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container grid">
      <div class="main">
        <div class="image">
          {c.image ? <img src={c.image} alt={c.title} loading="lazy" /> : null}
        </div>

        <h2 class="h2">Wat we hebben gedaan</h2>
        <ul class="list">
          {(c.overview || []).map((b) => <li>{b}</li>)}
        </ul>

        <h2 class="h2">Het resultaat</h2>
        <!-- Hier wordt nu de 'outcome' uit je cases.js geladen -->
        <p class="p outcome-text">
          {c.outcome}
        </p>
      </div>

      <aside class="side">
        <div class="panel">
          <h3 class="mini">Statistieken</h3>
          <div class="stats">
            {(c.stats || []).map((s) => (
              <div class="stat">
                <span class="label">{s.label}</span>
                <span class="value">{s.value}</span>
              </div>
            ))}
          </div>
        </div>

        <div class="panel">
          <h3 class="mini">Stack & Focus</h3>
          <!-- Hier worden nu de tags uit je cases.js getoond, gescheiden door een stip -->
          <p class="small">
            {(c.tags || []).join(' · ')}
          </p>
        </div>
      </aside>
    </div>
  </section>

  <style>
    /* ... Je bestaande styles blijven hetzelfde ... */
    
    .outcome-text {
      color: rgba(255,255,255,.8);
      font-size: 18px;
      line-height: 1.8;
      max-width: 65ch;
    }

    .hero{
      padding: 140px 0 70px;
      position: relative;
      overflow: hidden;
    }
    .hero::after{
      content:"";
      position:absolute;
      inset:-200px -120px;
      pointer-events:none;
      background:
        radial-gradient(1000px 620px at 20% 0%, color-mix(in srgb, var(--accent) 18%, transparent), transparent 62%),
        radial-gradient(900px 620px at 80% 70%, rgba(255,255,255,.06), transparent 70%);
      opacity:.9;
    }
    .inner{ position: relative; z-index: 1; max-width: 920px; }

    .kicker{
      margin: 0 0 14px;
      font-size: 12px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: rgba(255,255,255,.50);
    }

    .h1{
      margin: 0;
      font-size: clamp(40px, 5vw, 70px);
      letter-spacing: -0.05em;
      line-height: 1.02;
      font-weight: 650;
    }

    .lead{
      margin: 18px 0 0;
      color: rgba(255,255,255,.68);
      font-size: 18px;
      line-height: 1.8;
      max-width: 70ch;
    }

    .cta{
      margin-top: 22px;
      display:flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .grid{
      display:grid;
      grid-template-columns: 1.25fr .75fr;
      gap: 44px; /* Iets meer ruimte tussen de kolommen */
      align-items:start;
    }

    .image{
      border-radius: var(--radius);
      overflow:hidden;
      border: 1px solid rgba(255,255,255,.10);
      background: rgba(255,255,255,.03);
      margin-bottom: 32px;
    }
    img{ width:100%; height:auto; display:block; }

    .h2 {
      font-size: 24px;
      margin-bottom: 16px;
      margin-top: 32px;
    }

    .list{
      margin: 12px 0 0;
      padding-left: 18px;
      color: rgba(255,255,255,.72);
      line-height: 1.9;
    }

    .side{ display:flex; flex-direction:column; gap: 18px; }

    .panel{
      border-radius: var(--radius);
      background: rgba(255,255,255,.025);
      border: 1px solid rgba(255,255,255,.08);
      padding: 24px;
      position: relative;
    }

    .mini{
      margin: 0 0 16px;
      font-size: 11px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: rgba(255,255,255,.45);
    }

    .stats{
      display:grid;
      grid-template-columns: 1fr;
      gap: 12px;
    }
    .stat{
      border-radius: 12px;
      background: rgba(0,0,0,.2);
      border: 1px solid rgba(255,255,255,.05);
      padding: 12px 16px;
    }
    .label{
      display:block;
      font-size: 10px;
      letter-spacing: .12em;
      text-transform: uppercase;
      color: rgba(255,255,255,.4);
    }
    .value{
      display:block;
      margin-top: 4px;
      font-size: 16px;
      font-weight: 500;
      color: rgba(255,255,255,.9);
    }

    .small{
      margin: 0;
      font-size: 14px;
      color: rgba(255,255,255,.6);
      line-height: 1.6;
    }

    @media (max-width: 980px){
      .grid{ grid-template-columns: 1fr; }
    }
  </style>
</BaseLayout>